{
  "name": "facebook-auth-proxy",
  "version": "1.0.0",
  "description": "Proxy server for Facebook authentication",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.2",
    "dotenv": "^16.3.1"
    // "morgan": "^1.10.0" // Morgan có thể bỏ qua để tối giản nếu không cần logging nâng cao
    // Các thư viện bảo mật đã được loại bỏ
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
